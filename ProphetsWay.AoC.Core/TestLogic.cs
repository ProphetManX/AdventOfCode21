namespace ProphetsWay.AoC.Core.y2021.Day_00
{
    public class Logic : BaseLogic
    {
        public override string Part1(bool runSample = false)
        {
            return runSample
                ? "SAMPLE"
                : "TEST";
        }

        public override string Part2(bool runSample = false)
        {
            return runSample
                ? "SAMPLE"
                : "TEST";
        }
    }
}
